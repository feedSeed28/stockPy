/**
 * Runtime configuration for Ant Design Pro.
 * See: https://procomponents.ant.design/
 */

export const request = {
  // Axios request config — unified error handling will be added later
  timeout: 10000,
};
